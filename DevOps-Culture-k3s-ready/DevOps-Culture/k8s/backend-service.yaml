apiVersion: v1
kind: Service
metadata:
  name: taskflow-backend
  namespace: taskflow
  labels:
    app: taskflow
    component: backend
spec:
  selector:
    app: taskflow
    component: backend
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
