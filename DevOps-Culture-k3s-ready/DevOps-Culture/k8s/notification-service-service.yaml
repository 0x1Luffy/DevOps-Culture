apiVersion: v1
kind: Service
metadata:
  name: taskflow-notification-service
  namespace: taskflow
  labels:
    app: taskflow
    component: notification-service
spec:
  selector:
    app: taskflow
    component: notification-service
  ports:
  - name: http
    port: 5002
    targetPort: 5002
    protocol: TCP
  type: ClusterIP
